# Single source of truth: inherit the repo-root strict lint config so every
# workspace member is held to exactly the same bar. We own 100% of this code
# (no upstream), so there is one strict config, applied everywhere. Edit the
# rules in ../../analysis_options.yaml, not here.
include: ../../analysis_options.yaml
